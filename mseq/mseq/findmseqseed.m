% FINDMSEQSEED		Find seeds that generate maximal sequence
%
%   SEEDS = FINDMSEQSEED(ORDER) finds a list of seeds that generate maximal
%   sequences.  ORDER is the order of the sequence (i.e. sequence length is
%   2^ORDER).
%
% EXAMPLE:
%   seeds = findmseqseed(8);
%